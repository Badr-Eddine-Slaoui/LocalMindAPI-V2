services:
  host_pgadmin:
    image: dpage/pgadmin4:snapshot
    container_name: host_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: "${PGADMIN_EMAIL}"
      PGADMIN_DEFAULT_PASSWORD: "${PGADMIN_PASSWORD}"
    network_mode: host